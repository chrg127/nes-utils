chr.hpp is a small library for converting images to the NES graphics format (which usually
uses .chr or .bin extensions). The library can do conversion from images to NES graphics
by using the chr::convert() function, while it can do the reverse process using the
chr::extract() function.
